{
  "name": "RAG Policy Chatbot harness",
  "install": "python3 -c \"import sys; assert sys.version_info >= (3, 10), 'Python 3.10+ is required for the harness'; print('Python ' + sys.version.split()[0] + ' OK')\""
}
